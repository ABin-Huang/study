HDFS
1.目标：错误检测（CRC）和快速，自动的恢复



NameNode：中心服务器，负责管理文件系统的名字空间(namespace)以及客户端对文件的访问，
执行文件系统的名字空间操作，比如打开、关闭、重命名文件或目录。
也负责确定数据块到具体Datanode节点的映射，HDFS的元数据在这被管理

DataNode：通常一个节点（机器）一个，处理文件系统客户端的读写请求，管理它所在节点上的存储，
在Namenode的统一调度下进行数据块的创建、删除和复制。

